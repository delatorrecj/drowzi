# Dashboard image assets (placeholders in UI until these exist)

Drop files here and swap `require()` paths in `src/features/dashboard/DashboardMascotPlaceholder.tsx`.

| File | Purpose | Notes |
|------|---------|--------|
| `mascot-home-sleepy.png` | Default dashboard mascot (groggy / low streak) | Pixel-art, transparent PNG, ~512×512 |
| `mascot-home-amped.png` | High streak / energized variant | Same framing as sleepy for swap |
| `streak-flame.png` | Optional streak badge decoration | Small, ~64×64 |

Until assets ship, the app uses vector placeholder frames + labels.
