# Drowzi

Habit-gated wake-up app — see `docs/` (PRD, SDD, RFC, design).

## Mobile app

The Expo client lives in **`apps/mobile/`**.

```bash
cd apps/mobile
npm install
npm run start
```

- Architecture notes: `docs/sdd-drowzi.md`
- Two-dev hackathon workflow: `docs/plan-dev-workflow-split.md`

## Repo layout

```
apps/mobile/     Expo Router app (TypeScript)
docs/            Product & engineering docs
INIT.md          Brand / problem narrative
```
